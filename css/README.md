# CSS

Stylesheets for the public website.

Current convention:

- Keep global site styling in `style.css`.
- Prefer readable vanilla CSS.
- Avoid framework-specific naming or generated utility classes.
