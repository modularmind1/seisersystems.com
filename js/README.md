# JavaScript

Vanilla JavaScript for progressive enhancement.

Current convention:

- Do not add JavaScript unless static HTML and CSS are insufficient.
- Keep functions small and readable.
- Avoid frameworks, package managers, and build tooling.

Current files:

- `main.js` - Handles the email-based service review form.
